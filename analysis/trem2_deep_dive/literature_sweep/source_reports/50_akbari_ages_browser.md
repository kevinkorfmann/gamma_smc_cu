# 50 — Akbari AGES selection browser

- **Cite**: ❌ no separate citation — this row points to the same data as row 15 (Akbari 2026), now exposed via `https://reich-ages.rc.hms.harvard.edu/`
- **Method / cohort**: Vue SPA wrapping the per-variant Selection Summary Statistics table
- **TREM2 / TREML1 / 6:41 Mb**: identical to row 15 (max POSTERIOR 0.07 in TREM2 body, 0.10 TREML1, 0.60 in 41.0–41.3 Mb window — sub-threshold for 0.99 cutoff)
- **How checked**: confirmed live (200), API endpoints `/api/health`, `/api/search`, `/api/fetch`, `/api/generate_token` only; data endpoints token-gated; SPA bundle parsed at `/js/app.01de6390.js`. Underlying data IS the per-variant fixture used in row 15.
- **Confidence**: ❌ no new citation needed — already covered by `Akbari2026`
- **User action**: in the table, replace any pending citation with `\cite{Akbari2026}` and add the AGES URL as a footnote. Or drop the row as redundant.

**Notes.** Closes Round-6 Gap A2. ROUND_7 §A2.
