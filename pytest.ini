[pytest]
markers =
    betty: reproducibility tests that require betty HPC cache (auto-skipped off-cluster)
    slow: long-running tests (>1 min); skip with -m 'not slow'
    online: tests that hit external services (doi.org, NCBI); opt-in with -m online
